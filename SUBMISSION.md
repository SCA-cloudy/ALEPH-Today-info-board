[SUBMISSION.md](https://github.com/user-attachments/files/32084720/SUBMISSION.md)
# 제출문 — GrooveSpot (ALEPH 과제4 T04)

> 이 문서는 초안입니다. ②·③번 항목(직접 판단, AI 제안 미채택)은 본인 확인 후 다듬어 주세요.

## 결과물 URL

https://aleph-today-info-board.vercel.app/

## 소스 URL (커밋 고정)

*(⚠️ 아래는 예전 커밋 기준 예시입니다. TESTLOG.md·이 SUBMISSION.md를 GitHub에 올리면서 새 커밋이 여러 번 생겼으므로, 최종 제출 직전 GitHub Commits 탭에서 최신 커밋의 "Copy full SHA"로 전체 40자리 해시를 복사해 아래 형식으로 갱신하세요 — ALEPH 제출 양식은 짧은 해시가 아니라 반드시 전체 40자리를 요구합니다.)*

`https://github.com/SCA-cloudy/ALEPH-Today-info-board/commit/<전체 40자리 커밋 해시>`

## 짧은 확인 방법 (4줄)

1. **어디로 가나요**: https://aleph-today-info-board.vercel.app/ 를 새 시크릿 창에서 엽니다(로그인 불필요).
2. **3단계 이내 무엇을 하나요**: 히어로 카드의 곡명·아티스트·출처·출처 시각·조회 시각·"어제 대비"를 확인하고, 페이지 하단 "테스트 재생 모드"를 펼쳐 fixture JSON을 붙여넣은 뒤 "이 fixture 재생"을 누릅니다.
3. **무엇이 보이면 통과인가요**: 정상 fixture(D1-A/D1-B/D2)는 fresh/none 상태로 값·행수가 기대대로 바뀌고, 실패 fixture(timeout/401/429/offline/schema-break) 5종은 각각 다른 오류로 구분되어 "stale"과 함께 마지막 정상값이 그대로 남습니다. TIMEOUT 뒤 RECOVER-D2를 재생하면 fresh/none으로 복귀하고 다음 날짜 행이 1건 늘어납니다.
4. **안 될 때 무엇이 보이나요**: 실패 fixture 재생 후에도 값이 사라지거나 0/빈칸으로 바뀌면 실패이며, 복구(fixture 재생) 후에도 상태가 fresh로 돌아오지 않거나 행수가 늘지 않으면 실패입니다.

## AI-본인 판단 (3줄)

1. **AI에게 맡긴 일**: Apple Music 차트 API 연동(CORS 우회용 서버리스 프록시 `api/chart.js` 작성 포함), Supabase 스키마 설계·저장 로직·실패 5종 처리 로직 구현, 화면 코드 작성과 반복적인 디자인 수정, 테스트 중 발생한 버그(`source_observed_at` NOT NULL 오류, 다크모드에서 텍스트 안 보이던 문제 등) 진단·해결, fixture 재생 모드와 원클릭 재생·자동 비교·로그 저장 기능 구현, 실패 5종+복구 재생 테스트 및 비밀값 노출 스캔 수행.
2. **본인이 직접 판단한 일**: 다룰 데이터를 환율·날씨가 아닌 "음악 차트"로 직접 선택하고 signal을 Apple Music 인기차트(KR) 종합 1위로 확정, 디자인 레퍼런스(턴테이블 사진·에이전시 사이트 등)와 폰트 파일(온글잎 콘콘체 등)을 직접 제시하며 방향을 잡음, 서비스명 "GrooveSpot" 직접 결정, AI가 요청 없이 디자인을 임의로 진행했을 때 이를 지적하고 다시 방향을 상의하는 방식으로 되돌림, 강사·마스터 등 다른 사람이 평가하기 편하도록 fixture 원클릭 재생·자동 비교와 새로고침해도 남는 로그 저장 기능을 추가하도록 요청, 카드1·4·5의 실측 증거(Supabase SQL 조회·실제 화면 캡처)를 직접 실행·캡처해서 제공.
3. **AI 제안을 따르지 않은 일**: AI가 제안한 서비스명(트랙 일기·오늘의 그루브·바늘 다이어리 등)과 "스트리밍 재생 횟수"(Last.fm 연동) 기능을 채택하지 않음 — 이름은 직접 지은 "GrooveSpot"이 더 만족스러웠고, 재생 횟수는 무료 API로는 정확한 값을 못 구해 "정직한 데이터 표시"라는 과제 취지에 안 맞는다고 판단했기 때문. 또한 AI가 처음 "제출용 PDF는 필요 없다"고 판단했으나 실제 제출 폼의 파일 업로드 칸을 직접 확인하고 정정시켰고, 카드3 증거를 AI가 서술형 문장·표로만 정리하려 하자 실제 타임스탬프 로그를 직접 재생·캡처해서 넣도록 요구했으며, 제출확인자료 PDF 페이지 구성도 AI가 제안한 연속 배치 대신 카드별 페이지 분리 방식을 직접 선택함.

## 참고 — 카드별 확인 결과 요약

| 카드 | 상태 |
|---|---|
| 카드1 (값 하나 표시) | 완료 — 값·단위·출처·출처시각·조회시각·기준시간대(KST) 화면 표시 확인 |
| 카드2 (비밀 없는 호출) | 완료 — 브라우저 코드·배포 파일·네트워크 응답에서 비밀키 원문 0건 확인(자세히는 TESTLOG.md) |
| 카드3 (실패 5종) | 완료 — fixture 9종 재생 전부 기대값과 일치(자세히는 TESTLOG.md) |
| 카드4 (하루 한 줄) | 완료 — 같은 날 재실행은 갱신, 다음 날짜는 새 행(실제 데이터로도 확인됨) |
| 카드5 (실제 이틀·어제 대비) | 완료 — 서로 다른 실제 KST 날짜 2건(2026-09-10, 2026-09-11) 보존, 두 기록의 출처 URL·출처 시각·값·단위가 저장값·화면 표시값과 일치(2026-09-11은 화면 캡처로 직접 대조), "어제 대비" 재계산도 화면값과 일치 확인(자세히는 TESTLOG.md C22~C24) |
